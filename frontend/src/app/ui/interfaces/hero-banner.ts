export interface IHeroBanner {
    title: string;
    subtitle?: string;
    description?: string;
    backgroundImage?: string;
    displayButton?: boolean;
    video?: string;
    sponsorImage?: string;
}